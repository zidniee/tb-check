---
name: tb-phase2-clean-arch
description: Phase 2 - Scaffolds the Clean Architecture directory structure and base classes.
---

# Skill Execution Steps
1. Invoke the `tb-flutter-architect` context.
2. Create the Clean Architecture folders inside `lib/`: `domain/`, `data/`, `presentation/`.
3. Create placeholder base files (Entities, Repositories, UseCases) tailored to the TB App context (e.g., `PatientData`, `DiagnosisRepository`).
4. Stop execution and provide a summary to the user. Do not proceed to Phase 3 until instructed.
